# SECURITY WARNING: Never commit your actual API keys or secrets to git!
# This file is an example. Copy it to .env.local and fill in your values.

# Feature Flags
# Enable analytics (e.g., PostHog/GA). disabled by default.
VITE_ENABLE_ANALYTICS=false

# Waitlist Configuration

# Option A: Google Sheets
# 1. Create a Google Sheet
# 2. Add Apps Script to accept POST requests
# 3. Paste the Web App URL here
VITE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/PLACEHOLDER_ID/exec

# Option B: Resend (Requires server-side API implementation)
# These keys should ONLY be used in serverless functions (e.g., /api/waitlist.ts)
# DO NOT expose these to the client (Vite)
RESEND_API_KEY=re_123456789
RESEND_FROM_EMAIL=onboarding@resend.dev
